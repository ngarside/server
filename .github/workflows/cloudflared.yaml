# This is free and unencumbered software released into the public domain.

name: cloudflared

on:
  pull_request:
    paths: ['**/cloudflared.yaml', 'cloudflared/[st]*/**', 'pyproject.toml', 'scripts/**']
  push:
    branches: [master]
    paths: ['**/cloudflared.yaml', 'cloudflared/src/**']
  workflow_dispatch:

jobs:
  container_build:
    uses: ./.github/workflows/container_build.yaml
    permissions: {contents: read, packages: write}
    with: {path: cloudflared/src/cloudflared.dockerfile}
