# This is free and unencumbered software released into the public domain.

name: vector_unprivileged

on:
  pull_request:
    paths: ['**/vector.yml', 'vector/src/**', 'pyproject.toml', 'scripts/**']
  push:
    branches: [master]
    paths: ['**/vector.yml', 'vector/src/**']
  workflow_dispatch:

jobs:
  container_build:
    uses: ./.github/workflows/container_build.yml
    permissions: {contents: read, packages: write}
    with:
      name: vector-unprivileged
      path: vector/src/unprivileged.dockerfile
