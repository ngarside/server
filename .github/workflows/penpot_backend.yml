# This is free and unencumbered software released into the public domain.

name: penpot-backend

on:
  pull_request:
    paths: ['**/penpot_backend.yml', 'penpot/src/**', 'pyproject.toml', 'scripts/**']
  push:
    branches: [master]
    paths: ['**/penpot_backend.yml', 'penpot/src/**']
  workflow_dispatch:

jobs:
  container_build:
    uses: ./.github/workflows/container_build.yml
    permissions: {contents: read, packages: write}
    with:
      name: penpot-backend
      path: penpot/src/backend.dockerfile
